From 9417c5818a0146980c2608fda94c908e604eb033 Mon Sep 17 00:00:00 2001
From: Laxman Acharya Padhya <laxman.acharya.padhya@gmail.com>
Date: Mon, 21 Jul 2026 00:00:00 +0000
Subject: [PATCH] wifi: mt76: mt7921: validate CLC firmware records

Backport of upstream commit 9417c5818a0146980c2608fda94c908e604eb033
("wifi: mt76: mt7921: validate CLC firmware records"), not present in
Fedora 7.2.2 / openSUSE 7.2.

mt7921_load_clc() walks the CLC (country/location config) records
appended to the MCU firmware image and caches each in phy->clc[], which
is declared in mt792x.h as:

  void *clc[MT792x_CLC_MAX_NUM];   /* MT792x_CLC_MAX_NUM == 3 */

The index used is clc->idx, taken verbatim from the firmware blob and
never validated before subscripting that array. On a Framework Laptop 16
(MT7922) this fires on every boot:

  UBSAN: array-index-out-of-bounds in drivers/net/wireless/mediatek/mt76/mt7921/mcu.c:471:7
  index 3 is out of range for type 'void *[3]'
  Workqueue: events mt7921_init_work [mt7921_common]
  Call Trace:
   dump_stack_lvl+0x61/0x80
   ubsan_epilogue+0xc/0x40
   __ubsan_handle_out_of_bounds+0xab/0xb0
   mt7921_run_firmware+0x445/0x5a0 [mt7921_common]
   mt7921e_mcu_init+0xbe/0x110 [mt7921e]
   mt7921_init_work+0x51/0x5a0 [mt7921_common]

With UBSAN only reporting, the following "phy->clc[clc->idx] =
devm_kmemdup(...)" then writes a heap pointer past the end of the array,
corrupting the adjacent members of struct mt792x_phy.

Besides the idx bound, upstream also validates the region table against
the firmware size, uses underflow-safe length comparisons, checks that a
full record header is present before dereferencing it, and caches
clc->len for the loop increment - which additionally fixes an infinite
loop when a record reports len == 0.

Fixes: 23bdc5d8cadf ("wifi: mt76: mt7921: introduce ACPI SAR support")
Signed-off-by: Laxman Acharya Padhya <laxman.acharya.padhya@gmail.com>
Signed-off-by: Felix Fietkau <nbd@nbd.name>
---
 drivers/net/wireless/mediatek/mt76/mt7921/mcu.c | 34 ++++++++++++++----
 1 file changed, 27 insertions(+), 7 deletions(-)

diff --git a/drivers/net/wireless/mediatek/mt76/mt7921/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7921/mcu.c
--- a/drivers/net/wireless/mediatek/mt76/mt7921/mcu.c
+++ b/drivers/net/wireless/mediatek/mt76/mt7921/mcu.c
@@ -415,7 +415,8 @@ static int mt7921_load_clc(struct mt792x_dev *dev, const char *fw_name)
 	struct mt76_dev *mdev = &dev->mt76;
 	struct mt792x_phy *phy = &dev->phy;
 	const struct firmware *fw;
-	int ret, i, len, offset = 0;
+	size_t clc_len, fw_data_len, len, offset = 0;
+	int ret, i;
 	u8 *clc_base = NULL, hw_encap = 0;
 
 	dev->phy.clc_chan_conf = 0xff;
@@ -441,13 +442,21 @@ static int mt7921_load_clc(struct mt792x_dev *dev, const char *fw_name)
 	}
 
 	hdr = (const void *)(fw->data + fw->size - sizeof(*hdr));
+	if (hdr->n_region > (fw->size - sizeof(*hdr)) / sizeof(*region)) {
+		dev_err(mdev->dev, "Invalid firmware region table\n");
+		ret = -EINVAL;
+		goto out;
+	}
+	fw_data_len = fw->size - sizeof(*hdr) -
+		      hdr->n_region * sizeof(*region);
+
 	for (i = 0; i < hdr->n_region; i++) {
 		region = (const void *)((const u8 *)hdr -
 					(hdr->n_region - i) * sizeof(*region));
 		len = le32_to_cpu(region->len);
 
 		/* check if we have valid buffer size */
-		if (offset + len > fw->size) {
+		if (len > fw_data_len - offset) {
 			dev_err(mdev->dev, "Invalid firmware region\n");
 			ret = -EINVAL;
 			goto out;
@@ -464,8 +473,19 @@ static int mt7921_load_clc(struct mt792x_dev *dev, const char *fw_name)
 	if (!clc_base)
 		goto out;
 
-	for (offset = 0; offset < len; offset += le32_to_cpu(clc->len)) {
+	for (offset = 0; offset < len; offset += clc_len) {
+		if (len - offset < sizeof(*clc)) {
+			ret = -EINVAL;
+			goto out;
+		}
+
 		clc = (const struct mt7921_clc *)(clc_base + offset);
+		clc_len = le32_to_cpu(clc->len);
+		if (clc_len < sizeof(*clc) || clc_len > len - offset ||
+		    clc->idx >= ARRAY_SIZE(phy->clc)) {
+			ret = -EINVAL;
+			goto out;
+		}
 
 		/* do not init buf again if chip reset triggered */
 		if (phy->clc[clc->idx])
@@ -477,7 +497,7 @@ static int mt7921_load_clc(struct mt792x_dev *dev, const char *fw_name)
 			continue;
 
 		phy->clc[clc->idx] = devm_kmemdup(mdev->dev, clc,
-						  le32_to_cpu(clc->len),
+						  clc_len,
 						  GFP_KERNEL);
 
 		if (!phy->clc[clc->idx]) {
--
2.49.0
